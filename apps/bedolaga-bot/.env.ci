# Только для `docker compose config` в CI (не продакшен).
POSTGRES_DB=remnawave_bot
POSTGRES_USER=remnawave_user
POSTGRES_PASSWORD=ci_pg_pass
WEB_API_PORT=8080

BOT_TOKEN=dummy-token-for-compose-config-only-not-real
ADMIN_IDS=1

REMNAWAVE_API_URL=https://panel.invalid.example
REMNAWAVE_API_KEY=ci_key
REMNAWAVE_AUTH_TYPE=api_key
