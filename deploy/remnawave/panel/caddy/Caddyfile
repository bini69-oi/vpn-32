# Remnawave reverse proxy (Caddy)
# ВАЖНО: замените PANEL_DOMAIN и SUB_DOMAIN на реальные домены
# ПЕРЕД запуском Caddy.
# Caddy сам получит SSL через Let's Encrypt, если домены
# правильно резолвятся на IP сервера панели.
#
# Источник примеров: https://docs.rw/docs/install/subscription-page/bundled (секция Caddy)
#
# По умолчанию — прямой доступ к origin (без Cloudflare). Для reverse_proxy
# Caddy сам передаёт клиенту корректные X-Forwarded-* к upstream.
#
# За Cloudflare (orange cloud), если нужен реальный IP в приложении, замените
# блок reverse_proxy на вариант с:
#   header_up X-Real-IP {http.request.header.CF-Connecting-IP}
#   header_up X-Forwarded-For {http.request.header.CF-Connecting-IP}
# См. deploy/remnawave/docs/SECURITY_WIREFALL_CLOUDFLARE.md

https://PANEL_DOMAIN {
  reverse_proxy * http://remnawave:3000
}

https://SUB_DOMAIN {
  reverse_proxy * http://remnawave-subscription-page:3010
}
