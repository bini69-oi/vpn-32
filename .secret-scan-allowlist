# placeholders in docs/examples that look like secrets but aren't
archive/vpn-telegram-bot-custom/.env.example:BOT_TOKEN=replace-with-token-from-botfather
deploy/remnawave/panel/.env.example:JWT_AUTH_SECRET=
deploy/remnawave/panel/.env.example:JWT_API_TOKENS_SECRET=

# fixture token used in pytest (clearly fake, not a credential)
archive/vpn-telegram-bot-custom/tests/test_remnawave_client.py:fake-test-token

# archived vpn-productd example env — kept for historical reference only
archive/vpn-productd/env/vpn-productd.env.example:VPN_PRODUCT_API_TOKEN=change-me-strong-token

# CI-only dummy values for docker compose config (not credentials)
apps/bedolaga-bot/.env.ci:BOT_TOKEN=dummy-token-for-compose-config-only-not-real

# compose: официальное имя переменной POSTGRES_PASSWORD + placeholder (не секрет)
apps/bedolaga-bot/docker-compose.yml:      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-CHANGE_ME_STRONG_DB_PASSWORD}
apps/bedolaga-bot/docker-compose.yml:      POSTGRES_PASSWORD: '${POSTGRES_PASSWORD:-CHANGE_ME_STRONG_DB_PASSWORD}'
